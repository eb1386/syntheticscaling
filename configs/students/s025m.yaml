# Student s025m (16k BPE family, docs/07 amendment / user lock item 4).
# Derived by param_count.search_shapes(vocab=16384): total=25,175,424, non-emb=18,883,968, embedding share 25% (+0.7% of target).
# A 16k vocab (vs 32k) keeps embeddings from dominating the 25M model, so the
# student-size axis is not distorted by tokenizer overhead.
name: s025m
target_params: 25000000
n_layers: 12
d_model: 384
n_heads: 6
n_kv_heads: 2
head_dim: 64
d_ff: 1024
vocab_size: 16384
context_length: 2048
tie_embeddings: true
rope_theta: 10000.0
norm_eps: 1.0e-05
init_std: 0.02
depth_scaled_init: true
