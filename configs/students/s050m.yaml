# Student s050m (16k BPE family, docs/07 amendment / user lock item 4).
# Derived by param_count.search_shapes(vocab=16384): total=51,918,912, non-emb=42,481,728, embedding share 18% (+3.8% of target).
# A 16k vocab (vs 32k) keeps embeddings from dominating the 25M model, so the
# student-size axis is not distorted by tokenizer overhead.
name: s050m
target_params: 50000000
n_layers: 12
d_model: 576
n_heads: 9
n_kv_heads: 3
head_dim: 64
d_ff: 1536
vocab_size: 16384
context_length: 2048
tie_embeddings: true
rope_theta: 10000.0
norm_eps: 1.0e-05
init_std: 0.02
depth_scaled_init: true
