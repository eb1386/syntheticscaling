# TeacherConfig — Qwen/Qwen2.5-32B
# Role: instruct-vs-base ablation only (teacher_selection.md §5): 3-shot template, same decoding/filters
# Non-embedding parameters (teacher_selection.md §3, [UNVERIFIED to the last digit]): 31.0B
# exact_params is the safetensors total; re-check against the pinned revision at download time.
#
# >>> revision MUST be replaced by the HF commit SHA before any generation run (never 'main').
# >>> The launcher refuses to run generation while the placeholder is present.
name: t32b_base
family: qwen2.5
hf_repo: Qwen/Qwen2.5-32B
revision: PIN_ME_TO_COMMIT_SHA
nominal_params: 32000000000
exact_params: 32763876352
variant: base
weight_dtype: bf16
tensor_parallel: 1
max_model_len: 8192
chat_template: hf_default
thinking_mode: false
license: "Apache-2.0"
