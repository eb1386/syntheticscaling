# TeacherConfig — Qwen/Qwen2.5-3B-Instruct
# Role: primary 6-rung grid (doc 00 §0.6)
# Non-embedding parameters (teacher_selection.md §3, [UNVERIFIED to the last digit]): 2.77B
# exact_params is the safetensors total; re-check against the pinned revision at download time.
#
# >>> revision MUST be replaced by the HF commit SHA before any generation run (never 'main').
# >>> The launcher refuses to run generation while the placeholder is present.
name: t3b
family: qwen2.5
hf_repo: Qwen/Qwen2.5-3B-Instruct
revision: PIN_ME_TO_COMMIT_SHA
nominal_params: 3000000000
exact_params: 3085938688
variant: instruct
weight_dtype: bf16
tensor_parallel: 1
max_model_len: 8192
chat_template: hf_default
thinking_mode: false
license: "Qwen Research License (research use only)"
