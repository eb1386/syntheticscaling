# TeacherConfig — meta-llama/Llama-3.1-8B-Instruct
# Role: CROSS-FAMILY ANCHOR, ablation study only (doc 00 §0.6); never part of the primary grid
# Non-embedding parameters (teacher_selection.md §3, [UNVERIFIED to the last digit]): 6.98B
# exact_params is the safetensors total; re-check against the pinned revision at download time.
#
# >>> revision MUST be replaced by the HF commit SHA before any generation run (never 'main').
# >>> The launcher refuses to run generation while the placeholder is present.
name: llama8b
family: llama3.1
hf_repo: meta-llama/Llama-3.1-8B-Instruct
revision: PIN_ME_TO_COMMIT_SHA
nominal_params: 8000000000
exact_params: 8030261248
variant: instruct
weight_dtype: bf16
tensor_parallel: 1
max_model_len: 8192
chat_template: hf_default
thinking_mode: false
license: "Llama 3.1 Community License (naming clause applies to derived models)"
