# TeacherConfig second family (cross-family confound break, docs/24). meta-llama/Llama-3.1-70B-Instruct
# weight_dtype is the reference; serving quantization is set by the profile (int4/fp8 to fit).
# >>> replace revision with the HF commit SHA before any run (never 'main').
name: l70b
family: Llama-3.1
hf_repo: meta-llama/Llama-3.1-70B-Instruct
revision: PIN_ME_TO_COMMIT_SHA
nominal_params: 70600000000
variant: instruct
weight_dtype: bf16
tensor_parallel: 2
max_model_len: 4096
chat_template: hf_default
thinking_mode: false
license: llama3.1-community
